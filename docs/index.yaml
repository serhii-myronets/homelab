# Entry point. Read this first, then open only the file that answers the
# question - every fact below lives in exactly one place.
#
# Facts are YAML and verified against the live hosts. Reasoning is Markdown
# with YAML front matter under decisions/ and sessions/.
#
# The machines are named for what they are for, not for what they run:
#   beelink  gradual replacement for core        192.168.8.10   Talos Linux
#   core     the one nothing else may depend on  192.168.8.100  OpenMediaVault
#   proxmox  built and destroyed on purpose      10.1.1.100     Proxmox VE
#   router   the boundary with the internet      192.168.8.1    GL.iNet
#
# Two halves are deployed from here: core-talos/ onto the Beelink through Flux,
# and core/ onto core through Portainer - though every one of core's
# containers has been stopped since 2026-09-19 and is kept only as a way back.
# tools/ is neither: installers run by hand, reconciled by nobody, and
# archive/ holds setups switched off but kept whole, read by nothing.

meta:
  updated: 2026-09-21
  tags: [index, entrypoint, homelab]

files:
  - path: docs/hosts/beelink.yaml
    holds: >-
      Beelink ME Pro at 192.168.8.10 - the Talos node, its disks, and every
      service now running in the homelab. The busiest file here.
    tags: [beelink, talos, kubernetes, bare-metal, storage, services]
  - path: docs/hosts/core.yaml
    holds: OpenMediaVault box at 192.168.8.100 - hardware, disks, shares, cruft
    tags: [core, nas, omv, hardware, storage]
  - path: docs/hosts/proxmox.yaml
    holds: hypervisor at 10.1.1.100 - hardware, guests, spare capacity
    tags: [proxmox, hypervisor, lab]
  - path: docs/hosts/router.yaml
    holds: GL.iNet GL-MT6000 - VLANs, zones, DNS, DHCP, WireGuard, Tailscale, risks
    tags: [router, openwrt, glinet, firewall, vpn, tailscale]
  - path: docs/network.yaml
    holds: subnets, gateways, DNS, TLS and VPN routes - the parts that span hosts
    tags: [network, dns, tls, vpn, tailscale]
  - path: docs/backups.yaml
    holds: restic repositories, schedule, what is and is not covered
    tags: [backup, restic, disaster-recovery]
  - path: docs/services.yaml
    holds: core's stopped Docker stacks and their secrets - rollback material only
    tags: [core, docker, rollback, secrets]
    read_when: something has to be started on core again
  - path: docs/service-ideas.md
    holds: the shortlist of services not yet built, and where each might go
    tags: [ideas, services, backlog]
  - path: docs/traps.yaml
    holds: failures already paid for once - symptom, cause, tell, fix
    tags: [traps, gotchas, debugging]
    read_when: anything that should work does not
  - path: docs/decisions/
    holds: one file per decision, with status and tags in front matter
    tags: [decisions, adr, rationale]
  - path: docs/sessions/
    holds: what happened in past working sessions, newest last
    tags: [sessions, handoff, history]
    read_when: starting work - the newest one only

answers:                     # question -> file, so a lookup costs one read
  what is running right now: docs/hosts/beelink.yaml
  what hardware do I have: docs/hosts/
  what is the IP of X: docs/hosts/ or docs/network.yaml
  what does this repository deploy: core-talos/ onto the Beelink, core/ onto core
  what is installed by hand: tools/ - see decisions/0011
  why is it built this way: docs/decisions/
  what did we do last time: docs/sessions/
  what breaks if a disk dies: docs/backups.yaml
  what could I install next: docs/service-ideas.md
  which hostname serves what: docs/hosts/beelink.yaml, local_access and public_access
  what can reach what: docs/network.yaml, then hosts/router.yaml
  why is this failing: docs/traps.yaml
  how do I start core's old stack again: docs/services.yaml

rules:                       # the full set is in AGENTS.md
  - Verify against the live host before writing a fact down. Two documentation
    errors found on 2026-09-06 were both stale claims that read as true.
  - Do not edit the router's own uci config. See decisions/0007.
  - Secrets are never committed. docs/services.yaml lists their paths only.
  - A fact belongs in exactly one file. README.md is procedures for a human and
    does not restate these tables.
  - New failure mode -> traps.yaml. Choice with a rejected alternative ->
    decisions/. Narrative of a session -> sessions/.
  - Commits name one author, the owner. No agent attribution anywhere in the
    history.
