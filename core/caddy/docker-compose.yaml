services:
  caddy:
    image: caddy:2
    container_name: caddy
    networks:
      - backend
    ports:
      - "80:80"
      - "443:443"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      # Needs "Enable relative path volumes" on the Portainer stack, or the
      # path resolves inside Portainer's container where the daemon cannot see it.
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - /srv/ssd/docker/data/caddy/data:/data
      - /srv/ssd/docker/data/caddy/config:/config
    restart: unless-stopped

networks:
  backend:
    external: true