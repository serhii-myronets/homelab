resources:
  - namespace.yaml
  # Gateway API CRDs
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.0/standard-install.yaml
  # External Secrets reads this credential after its Helm release starts.
  - initial-secret.yaml
