apiVersion: apps/v1
kind: Deployment
metadata:
  name: jellyfin
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: jellyfin
  template:
    metadata:
      labels:
        app: jellyfin
    spec:
      initContainers:
        # A fresh or restored volume may be root-owned.
        - name: prepare
          image: busybox:1.37
          command: [sh, -c, "chown 1000:1000 /config /cache"]
          volumeMounts:
            - {name: data, mountPath: /config}
            - {name: cache, mountPath: /cache}
      containers:
        - name: jellyfin
          image: jellyfin/jellyfin:12.1.20260915-010956
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
          env:
            - {name: TZ, value: America/Los_Angeles}
          ports:
            - {name: http, containerPort: 8096}
          startupProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: {path: /health, port: http}
          livenessProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 30
          resources:
            requests: {cpu: 200m, memory: 512Mi}
            limits:
              memory: 3Gi
              # The iGPU, for Quick Sync transcoding.
              gpu.intel.com/i915: "1"
          volumeMounts:
            - {name: data, mountPath: /config}
            - {name: cache, mountPath: /cache}
            # The library Radarr and Sonarr build, as hard links to the files
            # qBittorrent is still seeding: the same data under a second,
            # ordered name. Jellyfin reads it and nothing else - the seeding
            # directory was mounted here until the last of it was imported on
            # 2026-09-20, and the two libraries now point at movies and tv
            # below. Read-only, because deleting is Sonarr's and Radarr's job.
            - {name: hdd-a, mountPath: /library, subPath: media, readOnly: true}
      volumes:
        - name: data
          persistentVolumeClaim: {claimName: data}
        - name: cache
          persistentVolumeClaim: {claimName: cache}
        - name: hdd-a
          persistentVolumeClaim: {claimName: hdd-a}
