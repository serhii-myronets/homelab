apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hdd-a
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: jellyfin-hdd-a
  resources:
    requests:
      storage: 9000Gi
---
# Transcodes and image caches: rebuilt on demand, so never backed up.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: lvm
  resources:
    requests:
      storage: 50Gi
