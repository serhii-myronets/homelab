apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: volsync-restic
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical-cloud
  target:
    name: volsync-restic
    template:
      data:
        # APP is replaced with the service's folder.
        RESTIC_REPOSITORY: "s3:{{ .ENDPOINT }}/homelab-backups/volsync/APP"
        RESTIC_PASSWORD: "{{ .RESTIC_PASSWORD }}"
        AWS_ACCESS_KEY_ID: "{{ .ACCESS_KEY_ID }}"
        AWS_SECRET_ACCESS_KEY: "{{ .SECRET_ACCESS_KEY }}"
  data:
    - {secretKey: ENDPOINT, remoteRef: {key: /backups/R2/ENDPOINT}}
    - {secretKey: ACCESS_KEY_ID, remoteRef: {key: /backups/R2/ACCESS_KEY_ID}}
    - {secretKey: SECRET_ACCESS_KEY, remoteRef: {key: /backups/R2/SECRET_ACCESS_KEY}}
    - {secretKey: RESTIC_PASSWORD, remoteRef: {key: /backups/R2/RESTIC_PASSWORD}}
