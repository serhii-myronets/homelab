apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    # node-exporter reads the host's /proc, /sys and root filesystem, which
    # the restricted policy forbids. Nothing else here needs it.
    pod-security.kubernetes.io/enforce: privileged
