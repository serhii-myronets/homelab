apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability
  namespace: argocd
spec:
  project: system
  sources:
    - repoURL: https://victoriametrics.github.io/helm-charts/
      chart: victoria-metrics-k8s-stack
      targetRevision: 0.93.0
      helm:
        valueFiles:
          - $values/core-talos/03-gitops/components/system/platform/observability/values.yaml
    # Supplies the values file and, as manifests, the namespace and the route.
    - repoURL: https://github.com/serhii-myronets/homelab.git
      targetRevision: main
      ref: values
      path: core-talos/03-gitops/components/system/platform/observability
      directory:
        include: "{namespace.yaml,httproute.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # Not optional here: the operator's CRDs carry 2.9 MB of schema, and a
      # client-side apply would try to write that into an annotation the
      # apiserver caps at 256 KB.
      - ServerSideApply=true
  # Grafana's chart mints a new admin password every time it renders, so
  # without this Argo would rewrite the secret on each sync, for ever.
  # Nothing reads it: the login form is off and access is anonymous.
  ignoreDifferences:
    - group: ""
      kind: Secret
      name: grafana
      namespace: observability
      jsonPointers:
        - /data
